/**
 * Frontend safety hooks.
 *
 * This product is an editing tool, so production code should not block browser
 * developer tools or run debugger traps. Keep this hook lightweight and only
 * use it for non-invasive protections.
 */
export function setupAntiScrape() {
  return undefined
}
